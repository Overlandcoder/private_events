<h1><%= @event.name %></h1>

<p><%= @event.date %></p>

<%= button_to "Attend Event", invitations_path,
    params: { invitation: { attendee_id: current_user.id, created_event_id: @event.id } },
    :method => :post %>
